//! GTK frontend boundary. GTK dependencies arrive with milestone M3.

#![forbid(unsafe_code)]

/// Launch the placeholder GUI boundary.
pub fn run() {
    helm_cli_fallback();
}

fn helm_cli_fallback() {
    println!("Helm GUI foundation");
}
